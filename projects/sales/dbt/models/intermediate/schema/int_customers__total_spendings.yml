models:
  - name: int_customers__total_spendings
    description: Exposes how much each customer has spent across all orders.
    columns:
      - name: customer_id
        description: Unique identifier for each customer.
        data_tests:
          - unique
          - not_null

      - name: total_spending
        description: How much each customer has spent across all orders.