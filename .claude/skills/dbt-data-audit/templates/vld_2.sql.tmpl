
/*
    vld_2 — which columns differ for {{BASE_NAME}}
    Generated by the dbt-data-audit skill on {{GENERATED_DATE}}.
    Output: one row per column with has_difference true/false (binary; magnitude not shown).
    {{FILTER_NOTE}}
    Next  : feed columns where has_difference = true into {{BASE_NAME}}_vld_3 for per-column stats.
*/

{% set old_query %} -- Legacy (A)
{{LEGACY_QUERY}}
{% endset %}

{% set new_query %} -- New (B)
{{NEW_QUERY}}
{% endset %}

{{
    audit_helper.compare_which_query_columns_differ(
        a_query = old_query,
        b_query = new_query,
        primary_key_columns = {{PRIMARY_KEY_COLUMNS_LIST}},
        columns = [
{{COLUMNS_LIST}}
{{EXCLUDED_COLUMNS_BLOCK}}
        ]
    )
}}
