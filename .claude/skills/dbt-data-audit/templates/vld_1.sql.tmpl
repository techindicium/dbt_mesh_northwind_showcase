
/*
    vld_1 — summary match for {{BASE_NAME}}
    Generated by the dbt-data-audit skill on {{GENERATED_DATE}}.
    Output: % of rows that are equal, only in A (legacy), only in B (new).
    {{FILTER_NOTE}}
    Next  : if equal % < 98, look at {{BASE_NAME}}_vld_2 to scope which columns differ.
*/

{% set old_query %} -- Legacy (A)
{{LEGACY_QUERY}}
{% endset %}

{% set new_query %} -- New (B)
{{NEW_QUERY}}
{% endset %}

{{
    audit_helper.compare_queries(
        a_query = old_query,
        b_query = new_query,
        primary_key = "{{PRIMARY_KEY}}",
    )
}}
